<%- include("partials/header") %>
    <main>  
        <h1>Overzicht games</h1> 
        <form action="/search" method="post">
            <label for="searchInput"></label>
            <input type="text" id="searchInput" name="query" placeholder="zoeken">   
            <button type="submit">zoeken</button>
        </form>
        <a href="/games">games</a> <a href="/developers">developers</a> <a href="/reset">reset</a> <a href="/logout">logout</a>
<%- include("partials/nav") %>
        <br>
            <% for (let i = 0; i < games.length; i++) { %>
                <ul id="json">
                    <% if (admin === "ADMIN") { %><a href="/edit/<%= games[i].name %>">Edit</a><% } %>
                    <li class="mainGame"><a href="/game/<%= games[i].id %>">
                        <%= games[i].name %> <br>
                        <img class="image" src="<%= games[i].imageUrl %>" alt="<%= games[i].name %>"></a>
                    </li>
                    <li class="mainGame"><%= games[i].releaseDate %></li>
                    <li class="mainGame"><%= games[i].metascore %></li>
                    <li class="mainGame"><%= games[i].genre %></a></li>
                    <li class="mainGame">
                        <%= games[i].gameWorld %>
                        <%= games[i].gameYear %>
                    </li>
                    <li class="mainGame"><%= games[i].thingsToDo %></li>
                    <li class="mainGame"><a href="/developer/<%= games[i].developer.id %>"><%= games[i].developer.name %></a></li>
                </ul> 
            <% } %>                              
    </main>
<%- include("partials/footer") %>
